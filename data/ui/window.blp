using Gtk 4.0;
using Adw 1;

template $AnuraWindow : Adw.ApplicationWindow {
  default-width: 480;
  default-height: 480;
  width-request: 360;
  height-request: 360;
  
  icon-name: "com.github.d3msudo.anura";

  [content]
  Adw.ToastOverlay toast_overlay {

    Gtk.Box {
      orientation: vertical;

      Adw.Banner portal_banner {
        title: _("Screenshot portal unavailable. Install xdg-desktop-portal-gtk (or the backend matching your desktop) and re-login.");
        button-label: _("Dismiss");
        revealed: false;
      }

      Adw.NavigationSplitView split_view {
        vexpand: true;
        collapsed: true;

        [sidebar]
        Adw.NavigationPage {
          title: _("Welcome");
          child: $WelcomePage welcome_page {};
        }

        [content]
        Adw.NavigationPage {
          title: _("Extracted Text");
          child: $ExtractedPage extracted_page {};
        }
      }
    }
  }
}
