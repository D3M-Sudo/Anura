# CLAUDE.md — Anura OCR Project Guide

> Thin compatibility pointer. The canonical AI-assistant guide is
> [AGENTS.md](AGENTS.md) — read that file instead. It is also read by
> Claude Code, Cline, Cursor, Aider, Continue, and Zed via the AGENTS.md
> convention. This file is kept only so setups that look for `CLAUDE.md`
> explicitly still land on the right guide.

Canonical sections now in `AGENTS.md`:

- Tech stack and versions → "Project Overview / Key Facts"
- Architecture and project structure → "Repository Structure", "Code Patterns & Conventions"
- Absolute agent rules (protected files, ruff-only, `uv` exclusive, i18n, early-return) → "Agent Rules — ABSOLUTE"
- Build, test (headless + GTK via system `python3-gi`), Flatpak → "Development Commands", "Testing"
- Security and privacy → "Security Guidelines" (+ `SECURITY.md`)
